import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    themeBB: "#9AC4F8",
    themeMint: "#99EDCC",
    themeRose: "#CB958E",
    themeBlush: "#E36588",
    themeJazz: "#9A275A",
  },
});
